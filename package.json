{
  "name": "task_project",
  "version": "1.0.0",
  "description": "This is a task application that is used to monitor task. It is simple to use. Just create a task. Mark as completed when done with the task. Then delete the task when it is not need.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "mongoose": "^6.0.0"
  }
}
